{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfile": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php-fpm -D && nginx -g 'daemon off;'"
  }
}
