{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfile": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "/app/start.sh && php-fpm -D && nginx -g 'daemon off;'"
  }
}
